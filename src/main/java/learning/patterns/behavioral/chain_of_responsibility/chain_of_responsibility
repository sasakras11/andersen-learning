це поведінковий паттерн який дозволяє передавати запроси послідовно по
цепочці обработчиків. Кожен наступний обработчик вирішує передавати запрос далі
чи перервати його. Клієнт може сам написати який обрабочик після якого буде виконуватия
це круто

Переваги:

1. зменшує залежність між клієнтом на обработчиками
2. реалізує принцип єдиного обов'язку ( single responsibility)
3. реалізує принцип open/close

Недоліки :

1. Запрос може залишитися ніким не обробленим

Використовуэться в java.util.logging.Logger#Log(), javax.servlet.Filter#doFilter