
він пропонує нам замінити наслідування агрегацією і композицією
Ми розділяємо одну нашу ієрархію на 2 частини - абстракцію та реалізацію

В прикладі Application без розділення абстракції(application) і реалізації(Developer)

у нас було б 4 класи - BankJava, BankPython, DomRiaJava ,DomRiaPython;
і якщо ми б добавили ще наприклад GoDeveloper - то було б уже не 4 а 6 різних реалізацій. І чим більше появляється
програм та програмістів - тим більше було б реалізацій.


Переваги - ми можемо круто комбінувати різні класи